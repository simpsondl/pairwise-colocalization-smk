# pairwise-colocalization-smk

This is a snakemake pipeline for performing colocalization using the coloc R package. The pipeline requires a file containing a list of variants across different summary statistics files.  
